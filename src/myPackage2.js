// @ts-check

/**
 * Intialize
 * @param {object} config
 * @param {string} config.url
 * @returns {boolean}
 */
export function init2(config) {
  return true;
}

/**
 * exit
 * @param {number} code
 * @returns {number}
 */
export function exit2(code) {
  return code + 1;
}
